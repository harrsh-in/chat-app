import { Box, Typography } from '@mui/material';

const Login = () => {
    return (
        <Box>
            <Typography variant="h4">Login</Typography>
        </Box>
    );
};

export default Login;

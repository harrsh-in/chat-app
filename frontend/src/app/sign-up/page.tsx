import { Box, Typography } from '@mui/material';

const SignUp = () => {
    return (
        <Box>
            <Typography variant="h4">SignUp</Typography>
        </Box>
    );
};

export default SignUp;

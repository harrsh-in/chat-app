import { Box, Typography } from '@mui/material';

const SignIn = () => {
    return (
        <Box>
            <Typography variant="h4">SignIn</Typography>
        </Box>
    );
};

export default SignIn;
